# =============================================================
#  Kronos Nginx 配置
#  路径: /etc/nginx/sites-available/kronos
#
#  有域名版（支持 HTTPS）请参考下方注释部分
# =============================================================

server {
    listen 80;
    server_name _;          # 有域名则改为: your-domain.com

    client_max_body_size 16M;

    # 访问日志
    access_log /var/log/nginx/kronos_access.log;
    error_log  /var/log/nginx/kronos_error.log;

    location / {
        proxy_pass         http://127.0.0.1:7070;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|ico|woff2?)$ {
        proxy_pass http://127.0.0.1:7070;
        add_header Cache-Control "public, max-age=86400";
    }
}


# =============================================================
#  HTTPS 版本（需先用 certbot 申请证书）
#  安装 certbot: apt install certbot python3-certbot-nginx
#  申请证书:     certbot --nginx -d your-domain.com
# =============================================================
#
# server {
#     listen 80;
#     server_name your-domain.com;
#     return 301 https://$host$request_uri;  # HTTP 强制跳转 HTTPS
# }
#
# server {
#     listen 443 ssl;
#     server_name your-domain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#
#     client_max_body_size 16M;
#
#     location / {
#         proxy_pass         http://127.0.0.1:7070;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 120s;
#         proxy_send_timeout 120s;
#     }
# }
